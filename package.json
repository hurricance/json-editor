{
  "name": "chrome-json-editor",
  "version": "1.0.0",
  "description": "Chrome extension: two-panel JSON editor with recursive formatting",
  "scripts": {
    "build": "esbuild src/background.ts src/editor.ts --bundle --minify --outdir=release/dist --format=iife && cp manifest.json editor.html release/ && cp -r icons release/",
    "typecheck": "tsc --noEmit",
    "watch": "esbuild src/background.ts src/editor.ts --bundle --outdir=dist --format=iife --watch",
    "package": "cd release && zip -r ../json-editor.zip ."
  },
  "devDependencies": {
    "@codemirror/autocomplete": "^6.20.1",
    "@codemirror/commands": "^6.10.3",
    "@codemirror/lang-json": "^6.0.2",
    "@codemirror/language": "^6.12.3",
    "@codemirror/state": "^6.6.0",
    "@codemirror/view": "^6.41.1",
    "@types/chrome": "^0.1.40",
    "esbuild": "^0.28.0",
    "lossless-json": "^4.3.0",
    "typescript": "^5.4.0"
  }
}
